/**
 * @wankong/integrations — connections to the outside world.
 *
 * Currently: a dependency-free MCP (Model Context Protocol) client over
 * Streamable HTTP, letting employees consume any MCP tool server as employee
 * tools. Credentialed SaaS connectors and OAuth flows join here next.
 */
export * from "./mcp.js";
