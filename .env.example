# --- AI providers (all optional) -------------------------------------------
# When a key is present, employees pinned to that provider use it. With no keys,
# the platform runs fully on the built-in hermetic `local` provider.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
# Provider used when an employee doesn't pin one (anthropic|openai|google|local).
DEFAULT_AI_PROVIDER=local

# --- API --------------------------------------------------------------------
PORT=4000

# --- Web --------------------------------------------------------------------
# The API is embedded in the web app and served at /api — no configuration
# needed. Set this only to point the console at an external API deployment.
# NEXT_PUBLIC_API_URL=https://api.example.com

# --- Data layer --------------------------------------------------------------
# Unset: the seeded in-memory store (dev/demo; resets on restart).
# Set to any Postgres connection string (Supabase/Neon/RDS...) and the durable
# Postgres store takes over automatically — schema is created and the demo org
# seeded on first boot. On Vercel, add this in Project Settings → Environment
# Variables (use your provider's pooled/"transaction" connection string).
DATABASE_URL=
